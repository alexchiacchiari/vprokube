apiVersion: v1
kind: Service
metadata:
  name: vproapp
spec:
  selector:
    app: vproapp
  ports:
    - protocol: TCP
      port: 8080 # External port
      targetPort: vproapp-port # Internal port
  type: ClusterIP
