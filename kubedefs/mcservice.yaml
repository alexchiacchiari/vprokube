apiVersion: v1
kind: Service
metadata:
  name: vprocache01
spec:
  selector:
    app: vpromc
  ports:
    - protocol: TCP
      port: 11211 # External port
      targetPort: vpromc-port # Internal port
  type: ClusterIP